{
  "private": true,
  "workspaces": [
    "example",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd example dev",
    "incept": "yarn --cwd example incept",
    "build": "yarn build:incept",
    "build:incept": "yarn --cwd packages/incept build"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}